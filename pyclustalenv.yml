name: pyclustalenv
channels:
    - defaults
    - conda-forge
dependencies:
    - numpy
    - regex
    - tqdm
    # Manipulate fasta files
    - biopython
    # Visualization in terminal
    - ete3 #Tree
    - tabulate #Numpy matrix
    # Logging
    - loguru


